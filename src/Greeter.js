//this is an example of using ES6 module
//we import and export files using the import and export syntax
function Greeter()  //either we can write export default function Greeter() here or we can write export default Greeter at the end of the program
{
    return <h2>Good Morning Ji </h2>
}

export default Greeter;
//use the same name for the function(component) as the name of the file and with first letter capital
